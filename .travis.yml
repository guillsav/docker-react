language: generic
sudo: required
services:
  - docker

before_install:
  - docker build -t guillsav/docker-react -f Dockerfile.dev .

script:
  - docker run -e CI=true guillsav/docker-react npm run test
